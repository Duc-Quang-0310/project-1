import React from 'react'
import iPage from '../interfaces/page'

export const Logging: React.FunctionComponent<iPage> = (props) => {
    return (
        <div>
            Loggin Page
        </div>
    )
}
